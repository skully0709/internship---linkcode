# Python packages — install with: pip install -r requirements.txt
gradio>=5.0.0
google-genai>=1.51.0
openai>=1.50.0
pydub>=0.25.1
python-dotenv>=1.0.1

# ffmpeg is required for voice input (audio format conversion) and is NOT a pip package.
#   Windows: winget install ffmpeg   (restart your terminal after installing)
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
